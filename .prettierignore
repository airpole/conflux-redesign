node_modules/
dist/
coverage/

# 명세 마크다운은 손으로 쓴 산문이다. 표 정렬과 줄바꿈이 의도된 것이고,
# 설계 대장은 테스트 파서가 읽는 서식이기도 하다. 포맷터가 건드리지 않는다.
**/*.md

# 관측 자료 — 손으로 고치지 않고 재생성한다.
tests/golden/

# 추출 스크립트는 원본을 읽는 도구이며 이 커밋의 스타일 규칙 밖이다.
tools/
